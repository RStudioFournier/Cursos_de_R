---
title: "Manipulacion de Base de Datos"
author: "Arnoldo Daniel Miranda Fournier"
date: "8/5/2018"
output: ioslides_presentation
---

Importación de datos
====================

##Importación de datos

![](Imagenes presentacion/Importación de datos1.png)

##Importación de datos

![](Imagenes presentacion/Importación de datos2.png)

##Importación de datos

![](Imagenes presentacion/Importación de datos3.png)

##Importación de datos

```{r comment=""}
base <- read.csv(
"~/Dropbox/Codigos en R/UAM/Primer Taller de estudiantes UAM-I y  Usuarios  de R-2018/2.-Manipulacion de Base de Datos/Bases de Ejemplos/Base 1 de Ejemplo(Ranavirus).csv")
```

##Importación de datos

```{r comment=""}
base <- read.csv(
"~/Dropbox/Codigos en R/UAM/Primer Taller de estudiantes UAM-I y  Usuarios  de R-2018/2.-Manipulacion de Base de Datos/Bases de Ejemplos/Base 1 de Ejemplo(Ranavirus).csv")
```

```{r comment="", eval=FALSE}
head(base)
```

##Importación de datos

```{r comment=""}
base <- read.csv(
"~/Dropbox/Codigos en R/UAM/Primer Taller de estudiantes UAM-I y  Usuarios  de R-2018/2.-Manipulacion de Base de Datos/Bases de Ejemplos/Base 1 de Ejemplo(Ranavirus).csv")
```

```{r comment=""}
head(base)
```

##Importación de datos

![](Imagenes presentacion/Importación de datos4.png)

##Importación de datos

```{r comment=""}
base <- read.delim(
"~/Dropbox/Codigos en R/UAM/Primer Taller de estudiantes UAM-I y  Usuarios  de R-2018/2.-Manipulacion de Base de Datos/Bases de Ejemplos/Base 1 de Ejemplo(Ranavirus).txt")
```

##Importación de datos

```{r comment=""}
base <- read.delim(
"~/Dropbox/Codigos en R/UAM/Primer Taller de estudiantes UAM-I y  Usuarios  de R-2018/2.-Manipulacion de Base de Datos/Bases de Ejemplos/Base 1 de Ejemplo(Ranavirus).txt")
```

```{r comment=""}
head(base, n = 8)
```

##Importación de datos

![](Imagenes presentacion/Importación de datos5.png)

##Importación de datos

```{r comment="", eval=FALSE}
base <- read.delim(file = "clipboard")
```

##Importación de datos

```{r comment="", eval=FALSE}
base <- read.delim(file = "clipboard")
```

```{r comment="", eval=FALSE}
base <- read.delim(file = pipe("pbpaste"))
```

##Importación de datos

```{r comment="", eval=FALSE}
base <- read.delim(file = "clipboard")
```

```{r comment="", eval= FALSE}
base <- read.delim(file = pipe("pbpaste"))
```

```{r comment=""}
head(base, n = 4)
```

##Importación de datos

#Desde R-Studio

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

## R Markdown

This is an R Markdown presentation. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document.

## Slide with Bullets

- Bullet 1
- Bullet 2
- Bullet 3

## Slide with R Output

```{r cars, echo = TRUE}
summary(cars)
```

## Slide with Plot

```{r pressure}
plot(pressure)
```

